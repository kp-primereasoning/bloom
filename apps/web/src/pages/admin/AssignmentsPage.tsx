import { PlaceholderPage } from '@/components/PlaceholderPage';

export function AssignmentsPage() {
  return (
    <PlaceholderPage
      title="Assignments"
      description="Assign florists to properties based on capacity and geography."
    />
  );
}
